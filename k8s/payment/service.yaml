apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: otel-demo
  labels:
    app: payment
    app.kubernetes.io/name: payment
    app.kubernetes.io/part-of: astroshop
spec:
  selector:
    app: payment
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
      protocol: TCP
  type: ClusterIP
